@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@using RestaurantMVCViewer.Data.Entities
@using RestaurantMVCViewer.Models.DTO
@using RestaurantMVCViewer.Models.ViewModels.DishesViewModels
@model SignatureDishesVM
@{
    ViewData["Title"] = "Index";
}

<h1>Signature dishes</h1>

<div class="row">
    @* @foreach(Restaurant restaurant in Model.Restaurants) // before using DTO*@
    @foreach (DishDTO dish in Model.Dishes)
    {
        <div class="col col-xl-3 col-lg-4 col-md-6 col-12 mb-5">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title text-truncate">@dish.DishName</h5>
                    <p class="card-text">@dish.DishDescription</p>
                    
                    <img src="data:image/*;base64,@(Convert.ToBase64String(dish.Image!))"
                         class="card-img-bottom"
                         alt="@dish.DishName">
                </div>
            </div>
        </div>
    }
</div>